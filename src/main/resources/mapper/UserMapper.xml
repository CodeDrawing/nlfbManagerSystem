<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.nlfb.ManagerSystem.mapper.UserMapper">
<select id="loginIn" resultType="top.nlfb.ManagerSystem.pojo.User" parameterType="top.nlfb.ManagerSystem.pojo.User">
    select * from user where userName=#{userName} and password=#{password}
</select>

</mapper>
